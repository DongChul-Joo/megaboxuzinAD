<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="event">
	
	<insert id="insertEvent" parameterType="com.zinsupark.event.Event">
		INSERT INTO event(ecode, userId, sdate, edate, subject, content,
		imageFilename, lott, lottDate, elink, mcount, ecategoryCode)
			VALUES (event_seq.NEXTVAL, #{userId}, #{sdate}, #{edate}, #{subject}, #{content},
					#{imageFilename}, #{lott}, #{lottDate}, #{elink}, #{mcount}, #{ecategoryCode})
	</insert>
	
	
	<sql id="where-list">
		<if test="condition=='all'">
			( INSTR(subject, #{keyword}) &gt; 0
				OR INSTR(content, #{keyword}) &gt; 0 )
		</if>
		<if test="condition=='subject'">
			INSTR(subject, #{keyword}) &gt; 0
		</if>
		<if test="condition=='content'">
			INSTR(content, #{keyword}) &gt; 0 
		</if>
		<if test="condition=='created'">
			( TO_CHAR(created, 'YYYY-MM-DD') = #{keyword}
				OR TO_CHAR(created, 'YYYYMMDD') = #{keyword})
		</if>
		
	</sql>
	
	<select id="listCategory" resultType="com.zinsupark.event.Event">
		SELECT eCategoryCode, eCategoryName 
		FROM eventCategory
	</select>
	
	<select id="dataCount" parameterType="map" resultType="Integer">
		SELECT NVL(COUNT(*), 0)
		FROM event e
		JOIN member m ON e.userId=m.userId
		<where>
			<if test="ecategoryCode!=0">
				ecategoryCode = #{ecategoryCode}
			</if>
			<choose>
				<when test="state==1">
				    AND edate &gt;= SYSDATE
				</when>
				<otherwise>
				    AND edate &lt; SYSDATE
				</otherwise>
			</choose>
			<if test="keyword != null and keyword != ''">
				AND <include refid="where-list"/>
			</if>
		</where>		
	</select>
	
	<select id="listEvent" parameterType="map" resultType="com.zinsupark.event.Event">
		SELECT ecode, e.userId, TO_CHAR(sdate, 'yyyy-mm-dd') as sdate, TO_CHAR(edate, 'yyyy-mm-dd') as edate,
		subject, TO_CHAR(created, 'yyyy-mm-dd') as created, imageFilename, lott, eLink, mCount
		FROM event e
		JOIN member m ON e.userId=m.userId
		<where>
			<if test="ecategoryCode!=0">
				ecategoryCode = #{ecategoryCode}
			</if>
			<choose>
				<when test="state==1">
				    AND edate &gt;= SYSDATE
				</when>
				<otherwise>
				    AND edate &lt; SYSDATE
				</otherwise>
			</choose>
			<if test="keyword != null and keyword != ''">
				AND <include refid="where-list"/>
			</if>
		</where>
		ORDER BY ecode DESC
		OFFSET #{offset} ROWS FETCH FIRST #{rows} ROWS ONLY
	</select>
	
	<select id="readEvent" parameterType="Integer" resultType="com.zinsupark.event.Event">
		SELECT ecode, subject, TO_CHAR(sdate, 'yyyy-mm-dd') as sdate, TO_CHAR(edate, 'yyyy-mm-dd') as edate,
				content, TO_CHAR(created, 'yyyy-mm-dd') as created, imageFilename, lott,
				TO_CHAR(lottDate, 'yyyy-mm-dd') as lottDate, elink, mcount
		FROM event e
		JOIN member m ON e.userId=m.userId
		WHERE ecode=#{ecode}
	</select>
	
	<update id="updateEvent" parameterType="com.zinsupark.event.Event">
		UPDATE event SET sdate=#{sdate}, edate=#{edate}, subject=#{subject}, content=#{content},
						imageFilename=#{imageFilename}, lott=#{lott}, lottDate=#{lottDate}, elink=#{elink},
						mcount=#{mcount}, ecategoryCode=#{ecategoryCode}
		WHERE ecode=#{ecode}
	</update>
	
	<delete id="deleteEvent" parameterType="Integer">
		DELETE FROM event WHERE ecode=#{ecode}
	</delete>
	
	<!-- 댓글 -->
	<insert id="insertReply" parameterType="com.zinsupark.event.Reply">
		INSERT INTO bbsReply(rcode, ecode, userId, content, created)
			VALUES (bbsReply_seq.NEXTVAL, #{ecode}, #{userId}, #{content}, #{created})
	</insert>

	<select id="replyCount" parameterType="map" resultType="Integer">
		SELECT NVL(COUNT(*), 0) FROM eventReply
		WHERE rcode=#{rcode} AND answer=0
	</select>
	
	<select id="listReply" parameterType="map" resultType="com.sp.bbs.Reply">
		SELECT r.rcode, r,userId, 
	</select>
	

</mapper>